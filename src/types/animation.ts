export interface LayerAnimationConfig {
  rotationSpeed: number // degrees per second
  pulseSpeed: number // cycles per second
  pulseAmplitude: number // scale multiplier amplitude (0 = no pulse)
}
